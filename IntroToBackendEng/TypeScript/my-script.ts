// we have learn that js is loosly designed lang its not a bug but a design which is for differet se cases in browser we have js so we cant use any other lang but we use code in ts convert it in between to js .
//as we have defined the data type just use . after the varible and it will give u dropdown for all related methods.improve dev experience


//apko sab define karna hoga 
function add(a:number , b:number) :number{
    return a+b
}

const result:number = add(3,5)
console.log(result)